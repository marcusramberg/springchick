[package]
name = "sc-config"
version = "0.1.0"
edition.workspace = true

[dependencies]
sc-shell-model = { path = "../sc-shell-model" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
